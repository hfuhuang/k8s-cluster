apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: jenkens
  name: jenkens_pvc
spec:
  storageClassName: local-storage
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi